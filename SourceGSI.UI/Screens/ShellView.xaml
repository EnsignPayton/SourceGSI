<Window x:Class="SourceGSI.UI.Screens.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:screens="clr-namespace:SourceGSI.UI.Screens"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance screens:ShellViewModel}"
        Title="{StaticResource Application_Title}"
        Height="480" Width="480">
    <StackPanel Background="{StaticResource SecondaryBrush700}">
        <Grid Height="64" Background="{StaticResource PrimaryBrush700}">
            <TextBlock Text="Dota 2 Game State"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="8"
                       FontSize="36"/>
        </Grid>
        <Label Content="{Binding GameState}"/>
        
        <Label Content="Hero Name"/>
        <TextBox Text="Abaddon"
                 Margin="8"
                 Padding="8"
                 IsReadOnly="True"/>
        
        <Label Content="Health"/>
        <TextBox Text="1592"
                 Margin="8"
                 Padding="8"
                 IsReadOnly="True"/>
    </StackPanel>
</Window>
